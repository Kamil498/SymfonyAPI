<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>O nas</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
          crossorigin="anonymous" />
    <style>
        /* Ciemne tło, kontrastowe kolory "programistyczne" */
        body {
            background-color: #121212;
            color: #c5c6c7;
            font-family: Consolas, Monaco, 'Courier New', monospace;
            min-height: 100vh;
            padding-top: 3rem;
        }

        /* Styl formularza */
        h1 {
            font-weight: 700;
            text-align: center;
            margin-bottom: 2rem;
            color: #66fcf1;
            text-shadow: 0 0 8px #45a29e;
        }

        .form-label {
            font-weight: 600;
            color: #45a29e;
        }

        .form-control {
            background-color: #1f2833;
            border: 1px solid #45a29e;
            color: #66fcf1;
            font-family: inherit;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-control:focus {
            background-color: #0b0c10;
            border-color: #66fcf1;
            box-shadow: 0 0 8px #66fcf1;
            color: #c5c6c7;
        }

        button[type="submit"] {
            background-color: #45a29e;
            border: none;
            color: #0b0c10;
            font-weight: 700;
            box-shadow: 0 4px 8px #45a29e88;
            transition: background-color 0.3s ease;
        }

        button[type="submit"]:hover {
            background-color: #66fcf1;
            color: #0b0c10;
            box-shadow: 0 6px 12px #66fcf1aa;
        }

        /* Link "Wróć" jako przycisk */
        .btn-back {
            background-color: transparent;
            border: 1px solid #45a29e;
            color: #45a29e;
            font-weight: 600;
            text-decoration: none;
            padding: 0.375rem 0.75rem;
            border-radius: 0.375rem;
            display: inline-block;
            margin-top: 1rem;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .btn-back:hover {
            background-color: #45a29e;
            color: #0b0c10;
            text-decoration: none;
        }

        /* Kontener formularza */
        .container-form {
            max-width: 600px;
            margin: 0 auto;
            padding: 1.5rem;
            background-color: #1f2833;
            border-radius: 0.75rem;
            box-shadow: 0 8px 24px rgb(0 255 255 / 0.15);
        }
    </style>
</head>

<body>
<main class="container-form">
    <h1>Dodaj do sekcji "O nas"</h1>
    <form method="post" action="{{ path('onas') }}">
        <div class="mb-3">
            <label for="tytul" class="form-label">Tytuł</label>
            <input name="tytul" id="tytul" type="text" class="form-control" required />
        </div>

        <div class="mb-3">
            <label for="opis" class="form-label">Opis</label>
            <textarea name="opis" id="opis" class="form-control" rows="6" required></textarea>
        </div>

        <button type="submit" class="btn btn-submit w-100">Dodaj</button>

        <a href="{{ path('list') }}" class="btn-back text-center d-block">Wróć</a>
    </form>
</main>

<!-- Bootstrap JS (opcjonalnie jeśli potrzebujesz np. JS formularza) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>

</html>
